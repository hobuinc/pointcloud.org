schema_version: 2

# LIKELY DUPLICATE: this folder's file is byte-identical in name and size to barringer/barringer-meteorite-crater-ncalm-2010-10.5069-G9V40S4C.copc.laz. Recommend confirming with Howard whether this folder should be removed rather than manifested as a second dataset.
# TODO: confirm license before publishing -- not yet verified for this dataset.
type: Collection
stac_version: 1.0.0
id: meteor-crater-duplicate
title: "Meteor Crater (duplicate of barringer-meteorite-crater)"
description: "TODO: write a one-line summary for Meteor Crater (duplicate of barringer-meteorite-crater)"
license: UNKNOWN
keywords: [arizona, ncalm, impact-crater, duplicate]
providers:
- name: NCALM
  roles: [producer]
  url: https://ncalm.cive.uh.edu/
  pointcloud_org:
    contact:
      name: Howard Butler
      email: howard@hobu.co
extent:
  temporal:
    interval:
    - - 2010-01-01
      - 
pointcloud_org:
  derivatives: true
  # Every asset below lives in our own Cloudflare R2 bucket, but this
  # is declared explicitly (rather than inferred from the "pointcloud"
  # bucket name) so every dataset uniformly states which S3-compatible
  # service+account hosts it -- AWS S3, this Cloudflare R2 account, or
  # someone else's bucket. Purely informational for this bucket: the
  # Worker always fetches "s3://pointcloud/..." via its native R2
  # binding, never over HTTP, so this value has no effect on fetching.
  default_endpoint: https://c88d624d8d8f065b1afce73bd44dcf1d.r2.cloudflarestorage.com
  viewer:
    enabled: true
    default_asset_id: barringer-meteorite-crater-ncalm-2010-10-5069-g9v40s4c

items:
- id: barringer-meteorite-crater-ncalm-2010-10-5069-g9v40s4c
  assets:
    data:
      href: s3://pointcloud/meteor/barringer-meteorite-crater-ncalm-2010-10.5069-G9V40S4C.copc.laz
      roles: [data]
  pointcloud_org:
    copc_resolution: 1
