schema_version: 2

# Filename encodes CRREL/RSGIS and an acquisition date of 2021-09-01. CRREL = US Army Cold Regions Research and Engineering Laboratory.
# TODO: confirm license before publishing -- not yet verified for this dataset.
type: Collection
stac_version: 1.0.0
id: boston-lot
title: Boston Lot
description: "TODO: write a one-line summary for Boston Lot"
license: UNKNOWN
keywords: [new-hampshire, crrel]
providers:
- name: CRREL
  roles: [producer]
  url: https://www.erdc.usace.army.mil/Locations/CRREL/
  pointcloud_org:
    contact:
      name: Howard Butler
      email: howard@hobu.co
      github_owner: hobu
extent:
  temporal:
    interval:
    - - 2021-09-01
      - 2021-09-01
pointcloud_org:
  derivatives: true
  # Every asset below lives in our own Cloudflare R2 bucket, but this
  # is declared explicitly (rather than inferred from the "pointcloud"
  # bucket name) so every dataset uniformly states which S3-compatible
  # service+account hosts it -- AWS S3, this Cloudflare R2 account, or
  # someone else's bucket. Purely informational for this bucket: the
  # Worker always fetches "s3://pointcloud/..." via its native R2
  # binding, never over HTTP, so this value has no effect on fetching.
  default_endpoint: https://c88d624d8d8f065b1afce73bd44dcf1d.r2.cloudflarestorage.com
  viewer:
    enabled: true
    default_asset_id: boston-lot-2021-09-01-crrel-rsgis

items:
- id: boston-lot-2021-09-01-crrel-rsgis
  assets:
    data:
      href: s3://pointcloud/boston-lot/boston-lot-2021-09-01-CRREL-RSGIS.copc.laz
      roles: [data]
  pointcloud_org:
    copc_resolution: 1
