{
  "name": "pointcloud-org-repo-scripts",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Root-level scripts for this repo's own CI checks (manifest validation, foreign-URL reachability) -- this repo has no site or ingest-backend code of its own, see manifests/README.md for how those are reached.",
  "scripts": {
    "validate-manifests": "node scripts/validate-manifests.mjs"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "js-yaml": "^4.1.0"
  }
}
