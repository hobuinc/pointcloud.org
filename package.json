{
  "name": "pointcloud-org-repo-scripts",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Root-level scripts shared across CI workflows (manifest validation, site deploy) -- NOT the Worker or the site, see worker/package.json and site/package.json for those.",
  "scripts": {
    "validate-manifests": "node scripts/validate-manifests.mjs"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "js-yaml": "^4.1.0"
  }
}
